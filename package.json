{
  "name": "safelogins",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd client && npm run start",
    "install": "cd client && npm i",
    "build": "cd client && npm run build",
    "install:build": "npm run install && npm run build",
    "deploy": "npm run install:build && cd client && npm run deploy"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/andrewaclark248/SafeLogins.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/andrewaclark248/SafeLogins/issues"
  },
  "homepage": "https://github.com/andrewaclark248/SafeLogins#readme",
  "dependencies": {
    "concurrently": "^8.2.0",
    "typescript": "^5.1.6"
  },
  "react-dotenv": {
    "whitelist": ["FIREBASE_API_KEY", "FIREBASE_AUTH_DOMAIN", "FIREBASE_PROJECT_ID", "FIREBASE_STORAGE_BUCKET", "FIREBASE_MESSAGING_SENDER_ID", "FIREBASE_APP_ID", "SAFE_TEXTING_API"]
  }
}
